/**
 * Single-image live preview: letterboxed and centered in the media frame (LinkedIn-style).
 * Use platform-specific overrides when a mockup needs different behavior (e.g. LinkedIn top anchor).
 */
export const PREVIEW_SINGLE_IMAGE_CENTERED =
  "h-full w-full min-h-0 max-h-full object-contain object-center";
