import type { ReactElement } from "react";

import { MaterialSymbolsLink } from "@/components/fonts/MaterialSymbolsLink";

/** @deprecated Prefer `MaterialSymbolsLink` — kept for existing workspace imports. */
export function WorkspaceMaterialSymbolsLink(): ReactElement {
  return <MaterialSymbolsLink />;
}
