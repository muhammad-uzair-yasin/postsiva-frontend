import { WordPressSelfHostedHelpContent } from "@/components/help/WordPressSelfHostedHelpContent";

/** Public help page — same content as the in-app setup guide modal. */
export function WordPressSelfHostedHelpArticle(): React.ReactElement {
  return <WordPressSelfHostedHelpContent />;
}
