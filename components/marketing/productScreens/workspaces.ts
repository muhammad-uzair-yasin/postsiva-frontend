import type { StaticImageData } from "next/image";

import img from "@/assets/images/workspaces.png";

/** `workspaces.png` — workspace picker */
export const marketingImageWorkspaces: StaticImageData = img;
