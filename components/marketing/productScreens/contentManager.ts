import type { StaticImageData } from "next/image";

import img from "@/assets/images/conten_manager.png";

/** `conten_manager.png` — content manager */
export const marketingImageContentManager: StaticImageData = img;
