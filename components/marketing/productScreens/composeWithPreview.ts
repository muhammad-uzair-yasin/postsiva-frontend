import type { StaticImageData } from "next/image";

import img from "@/assets/images/compose_with_preview.png";

/** `compose_with_preview.png` — composer with previews */
export const marketingImageComposeWithPreview: StaticImageData = img;
