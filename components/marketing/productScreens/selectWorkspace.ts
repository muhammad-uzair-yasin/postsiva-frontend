import type { StaticImageData } from "next/image";

import img from "@/assets/images/select_workspace.png";

/** `select_workspace.png` — choose workspace UI */
export const marketingImageSelectWorkspace: StaticImageData = img;
